# domain

Pure, framework-free business core. Entities (`Product`) and value objects (`Money`) with
their invariants, plus the domain error hierarchy.

**Depends on:** nothing (no NestJS, no Elasticsearch/Redis, no DTOs).
