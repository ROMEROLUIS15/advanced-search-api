# Dependencies
node_modules/

# Build output
dist/
build/

# Test / coverage
coverage/
.nyc_output/

# Environment — never commit secrets (.env.example IS tracked)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Artifacts of a local production-shape run (see the CI job of the same name):
# the captured stdout is covered by *.log above, these are the rest.
loki-received.jsonl
*.pid

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Local scratch notes
.notes/
