# Outstanding work — 2026-07-28

State of the repository after the session of 2026-07-27/28, written as a handoff. Everything here was
verified against the code, the deployed service or Grafana Cloud on 2026-07-28; nothing is recalled from
memory. Where a claim could not be verified, it says so.

> **Updated at the end of 2026-07-28.** The day closed **B5, C, E1–E8, B1, H1, H2 and B4** — two OpenSpec
> changes archived (`ship-metrics-and-logs-to-grafana`, `split-health-liveness-and-readiness`), ten
> capabilities in `openspec/specs/` — the second change modified `service-health` rather than adding one.
>
> Each fix uncovered the next. Switching log shipping on exposed two silent wire-format defects no suite
> could reach (`7ee3757`). Fixing the keep-alive — GitHub had been delivering ~1 run/hour since the cron
> shipped, so an external 5-minute monitor took over — kept the instance genuinely awake for the first time,
> which revealed that Render health-polls every ~4.3 s and put the Upstash free tier ~25 days from a breach
> (H2). Splitting readiness from the full report removed that: the platform now polls a critical-only
> `/health/ready`, health-driven Redis traffic is **70× lower**, verified in production.
>
> Still open: **one console reading inside H3**, and nothing else. §A is closed by the owner's decision —
> this working copy is disposable and moving machines — and is not to be re-raised; B2, D1, D2, D3 and D4
> closed on the 28th, and **B3 on 2026-07-29** with the versioned-migration change. The updated order is at
> the bottom.
>
> H3 is the one worth reading before assuming it is another quota emergency: the Elastic side has **no
> per-request billing dimension at all**, so the Upstash-shaped failure cannot repeat there. Measured, with
> the pricing verified at the source — see §H.

The item-by-item state below is current; where an item was closed, its section says so and keeps the
original text for the record.

## What shipped, so this list is read in context

An external audit raised 17 findings. Sixteen were confirmed, one (`/health` sub-paths bypassing the
limiter) was mis-attributed — every unmatched path behaves that way, because unmatched routes never reach a
Nest guard. Of the confirmed ones, eleven are now closed:

readiness proves the index exists · probe failure details no longer published · `METRICS_TOKEN` required in
production · `API_KEYS` minimum length · zero TTL no longer writes `EX 0` · a missing alias is repaired
instead of reported as success · query strings out of logs and traces · cache keys scoped to their index and
ranking · autocomplete keys hashed · Redis actually traced · metrics shipped to Grafana Cloud.

One defect was found *by the fix*: once Redis was traced, `db.query.text` turned out to carry the full
command, which on the cache path would have shipped entire result sets to Tempo. Fixed in `f23afe7`.

---

## A. Local credentials in `.env` — CLOSED by decision, 2026-07-28

**Owner's decision, recorded so it is not re-raised:** this working copy is a local test checkout. The
project is being cloned to another machine and this copy — `.env` included — will be deleted, so the
credentials in it go away with it. The OneDrive-sync exposure is accepted on that basis and **needs no
rotation task here**. Do not reopen this section; if a credential ever needs rotating it will be for an
operational reason (a leak, a staff change, a provider requirement), not because of this folder.

For the record, so the next reader knows what was in scope: `.env` held five secrets — the Elastic Cloud
API key, the Upstash password inside its `rediss://` URL, the Grafana Cloud token twice (once as a
commented `GRAFANA=` line and once live inside `OTEL_EXPORTER_OTLP_HEADERS`), and the API's own two client
keys. `.env` is git-ignored and a full-history gitleaks scan is clean, so nothing of this ever reached the
repository.

**One operational fact survives this section and belongs elsewhere:** the Grafana Cloud token is used by
**two** Render variables — `LOKI_PASSWORD` (log shipping) and inside `OTEL_EXPORTER_OTLP_HEADERS` (traces
and metrics). Whenever it is replaced, both must be updated together or one pipeline stops shipping
silently, since both are best-effort by design. Tracked with the deployment gaps in §D, not here.

---

## B. Code

### B1 — Metrics do not count 401, 429 or 404 · *highest value of the five* · **CLOSED 2026-07-28**

**Done in `e464ca6`, exactly as the approach below describes**: recording moved to an Express middleware in
`app.setup.ts`, ahead of the router and therefore of every guard; the interceptor (and its false docstring)
retired in the same commit, so nothing double-counts. Bonus the approach did not promise: traffic to the
Express-mounted `/docs` now lands in the `unmatched` bucket, shrinking B2's observability blind spot. An e2e
case asserts a guard-rejected 401 lands in the registry under its matched route. The original finding is kept
below for the record.

`MetricsInterceptor` is a Nest interceptor, so it runs **after** the guards. A request rejected by the API-key
guard or the rate limiter never reaches it, and neither does an unmatched route. The abuse signals are exactly
the ones not measured.

Confirmed on 2026-07-28: four `GET /search` requests without a key returned 401, and **none** of them appear
in `http_requests_total` in Grafana Cloud.

The interceptor's own docstring claims it "records the RED metrics for every request", which is wrong and
should be corrected with the fix.

**Approach**: measure in an Express middleware registered in `app.setup.ts`, ahead of the guards, hanging off
the response's `finish` event as the interceptor already does. Keep the route label as the *matched pattern*
(`request.route?.path`), falling back to a single `unmatched` bucket — labelling by raw URL would create one
series per distinct query. Retire the interceptor or reduce it to the part the middleware cannot see.

Watch out: the middleware must not double-count with the interceptor during the transition, and `/metrics`
itself is inside the rate limiter, so its own requests get counted.

### B2 — Swagger is outside the pipeline · **CLOSED 2026-07-28**

Closed in three steps across the day, which is why the entry below reads worse than the situation ended up
being. The metrics middleware (`e464ca6`) put docs traffic into the registry under `unmatched`; the
`production-shape` CI job (`e3a7e5b`) started exercising the `/docs-json` key check; and the rate limiter now
covers the docs paths as a middleware registered **before** that key check, in its own `docs:` namespace
against the `default` budget — mirroring why `RateLimitModule` is imported before `ApiAuthModule`, since an
unauthenticated flood has to be counted rather than rejected for free.

Verified against the real binary rather than reasoned about: with the budget set to 3, an unauthenticated
burst answered `401, 401, 401, 429, 429, 429` — the attempts are metered — a valid key from that same
exhausted address still got **200**, so a flood cannot lock out a legitimate holder behind one NAT, and
`/search` was untouched, so the docs namespace spends nobody else's budget. Pinned by five unit cases and two
smoke cases.

Still true and deliberately not changed: docs traffic produces no request-log line, since `LoggingInterceptor`
is a Nest interceptor and these are not Nest routes. The original entry follows.

`swagger.setup.ts:32` mounts `/docs` and `/docs-json` with `app.use`, so no Nest guard, interceptor or
filter runs for them. Consequences: unlimited API-key attempts against the docs, and no metrics or logs for
that traffic. The key check itself is sound (SHA-256 + `timingSafeEqual`), so brute force is not a realistic
vector; the operational blind spot is the real cost.

**Approach**: either a small rate-limiting middleware sharing `RateLimitStorePort`, or serve the document
from a guarded controller instead of `SwaggerModule.setup`.

### B3 — Index migration is not really versioned · **CLOSED 2026-07-29**

Closed by the OpenSpec change `add-versioned-index-migration` (D43–D49). `PHYSICAL_INDEX_SUFFIX` is gone:
the seed hashes the index definition, compares it with the digest recorded in the live index's `_meta`, and
on a mismatch builds `<alias>_v<n+1>`, loads the dataset into it and moves the alias in **one**
`updateAliases` action list. Retired products disappear because the new index receives exactly the dataset,
and one previous version is retained so a rollback is an alias move (runbook in the README).

Two things were verified rather than assumed, both of which could have sunk the design quietly:
**Elastic Cloud Serverless behaves identically to the local 8.17** on `_meta` round-trip, the atomic flip and
`indices.delete` (probed against the production project with a throwaway alias, cleaned up afterwards); and a
concurrent `indices.create` answers **400**, not the 409 one would expect, so the guard keys on the error
type. The migration was then rehearsed end to end locally: `products_v1` (no fingerprint, exactly like
production) migrated to `products_v2`, the alias moved, v1 was retained, and a second run reported
"unchanged" and did nothing.

**What did NOT change, deliberately**: the seed is still a manual pre-boot step — automating it is a separate
decision with its own failure mode and stays open. And cache invalidation is still left to TTL (D49): scopes
digest the alias, which a migration does not change, so results written before a flip stay servable for up to
`CACHE_TTL_SEARCH` (300 s). Both are stated in the design so neither reads as an oversight.

**One consequence to expect on the next production seed**: `products_v1` carries no fingerprint, so the first
run migrates for real to `products_v2` rather than being a no-op. Intended, rehearsed, and the reason the
integration suite polls the alias *during* the flip.

### B4 — `/health` is unauthenticated and unthrottled · **substance closed 2026-07-28**

Closed by `split-health-liveness-and-readiness`, though **not the way this entry proposed**. The sketch below
says to keep only `/live` fully open; that is not achievable, because Render's check is the deploy gate and
it sends no credential — polling a dependency-free `/live` would let an unseeded deployment go live and serve
503s instead of failing the deploy. So all three health endpoints stay open, and the fan-out concern is
answered instead by making the continuously polled path touch **one** dependency rather than two. The
original text is kept below for the record.

Exempt from both the API-key guard and the rate limiter, and every call fans out to a parallel Elasticsearch
and Redis probe. Severity is **low**, and deliberately so: measured on the Upstash console on 2026-07-28, the
whole database served **3,100 commands for the month** against a 500,000 free-tier allowance, so nothing is
being consumed at a rate worth defending against. An earlier estimate of ~518,000 was wrong by 170× and has
been withdrawn — see the correction in `docs/OBSERVABILITY-2026-07-27.md`.

**Approach when it comes up**: split `/live` (no dependencies) from `/ready` (dependencies), and keep only
`/live` fully open.

**Revised 2026-07-28 by H2**: the "3,100 commands for the month" figure — and the low severity resting on
it — silently assumed the instance slept most of the day (H1). Awake 24/7, Render's own ~4.3 s health
polling spends ~605k Upstash commands/month on its own. See H2 for the measured numbers and options.

### B5 — Log shipping is implemented but not switched on · **CLOSED 2026-07-28**

**Switched on and verified** — but not by just setting the variables: the first activation silenced stdout
*and* Loki at once, because the logger's readable `level` and `time` formats each break a different half of
the worker-transport pipeline, invisibly. Fixed in `7ee3757` (numeric wire format across the worker), lines
verified in Loki by `correlationId`, and the Tempo pivot executed against production. The measured numbers
and the full mechanism are in `docs/OBSERVABILITY-2026-07-27.md`. The variable block below stays valid.

The code is deployed and inert. Set on Render to activate:

```
LOKI_URL=https://logs-prod-<region>.grafana.net
LOKI_USERNAME=<numeric Grafana Cloud user id>
LOKI_PASSWORD=<Grafana Cloud token>
```

`LOKI_USERNAME` and `LOKI_PASSWORD` must be set together or not at all; the schema rejects half a credential
because Loki would answer 401 to every batch and shipping is best-effort, so the failure would be invisible.

---

## C. Closing the OpenSpec change · **CLOSED 2026-07-28**

Archived as `openspec/changes/archive/2026-07-28-ship-metrics-and-logs-to-grafana` (28/28 tasks), delta
specs synced — `telemetry-shipping` is the tenth capability — and all ten specs validate `--strict`.

- [x] **4.6** Verified: the request's line is in Loki with `correlationId="claude-46-pivot"`, and Tempo trace
      `f9016e697abd47e01a45cad023667ce9` pivots to it by its end instant (millisecond match with the line's
      `time`). The pivot is temporal, not attribute-linked — recorded as such.
- [x] **5.1** Measured: **94 active series** vs ~400 estimated; **43 lines / 7,054 bytes** in the first
      active hour ≈ ~2 MB/month vs <10 MB estimated. Appended to `docs/OBSERVABILITY-2026-07-27.md`.
- [x] **5.2** Not needed — both signals orders of magnitude inside the free tier.

`search_cache_events_total` was observed for the first time: `hit=1, miss=2` after one repeated authenticated
search. The cache story is now visible end to end.

---

## D. Process and CI

- [x] **D1** · **CLOSED 2026-07-28.** `render.yaml` had `autoDeploy: true` with no CI gate; deploys started
      on push and finished before the checks did, twice that day, both passing by luck rather than by gate.
      Now `autoDeployTrigger: checksPass`, which supersedes the deprecated boolean and is native to the
      blueprint — no deploy hook and no GitHub secret needed. Worth knowing before relying on it: Render
      skips the deploy if any check fails **and also if no checks exist at all**, so the gate is only as real
      as the repo's checks, and this repo runs CI, CodeQL and Security on every push to main. The cost is
      latency — the Security job's ZAP scan takes ~3 minutes, so a deploy lands minutes after the push rather
      than seconds.
      *Note for anyone reading Render's docs on this: the "Deploys" page says the setting is dashboard-only.
      The blueprint spec contradicts it, and the API settles it — `list_services` already reported
      `autoDeployTrigger` on this service. The field is real.*
      **The gate does not apply to the push that introduces it.** Measured: the push carrying this change
      deployed at the same second it landed, under `trigger: new_commit`, and CI went red minutes later — a
      red commit reached production. Render only syncs the blueprint after the push, so the deploy for that
      push is already away under the old rule. `list_services` confirmed `autoDeployTrigger: checksPass`
      immediately afterwards, so the gate governs every push from the next one on. Expect this once, on any
      future change to this field.
- [x] **D2** · **CLOSED 2026-07-28** (`e3a7e5b`). No job booted the app the way Render does: `quality` mocks
      its ports, `integration` runs `configureApp` with the gate off, and DAST turns auth and the limiter off
      so ZAP can reach the surface. The gap drew blood the same week — the log-shipping defects (`7ee3757`)
      were unreachable by every suite because none ran with `LOKI_URL` set, so the first execution of the
      real transport worker was the production deploy.
      A third CI job, **`production-shape`**, now boots `dist/main.js` with authentication on, the limiter on
      with a small `/search` budget, Swagger mounted and `LOKI_URL` pointed at `scripts/loki-sink.mjs` (a
      throwaway HTTP recorder — catching this needs a socket that records, not a real Loki). It asserts the
      two halves a broken wire format breaks separately: stdout still carries JSON lines with a transport
      attached, and the sink received a batch with the expected labels. HTTP assertions are in
      `test/production.smoke-spec.ts` (`npm run test:smoke`), so they are linted and type-checked and can be
      aimed at any instance with `SMOKE_BASE_URL` / `SMOKE_API_KEY`; they also cover the `/docs-json` guard,
      which is Express middleware no Nest guard runs and no e2e boot mounts.
      **Two doc corrections fell out of writing it**: a throttled response carries `Retry-After` and *no*
      `RateLimit-*` headers (those ride the success path only), and pino-loki adds a `level` label of its own
      beside the configured `service` and `env` — bounded at six values, so harmless, but not "service and
      env only" as CLAUDE.md had said.
- [x] **D3** · **CLOSED 2026-07-28** (`343d0b7`). Mutable supply chain: actions pinned to major tags
      (`actions/checkout@v7`, `github/codeql-action@v4`), `zricethezav/gitleaks:latest`,
      `ghcr.io/zaproxy/zaproxy:stable` — and the two scanner steps run with the whole repository mounted, so
      whoever controls those tags controls what reads the repo. Everything now pins an immutable reference:
      actions by commit SHA with the version in a trailing comment, both images by `version@digest`.
      Behaviour is unchanged by construction — `:latest` resolved to gitleaks **v8.30.1** and `:stable` to
      zaproxy **2.17.0** at the moment of pinning, digests read from Docker Hub and ghcr rather than copied
      from a README.
      **What to know afterwards**: Dependabot's github-actions ecosystem understands the SHA-plus-comment
      form and bumps both together, so the actions cost no staleness — but it cannot see an image named in a
      `run:` step (it reads the Dockerfile and, inside workflows, only `container:`/`services:`). Those two
      digests are therefore manual; the `docker manifest inspect` line to refresh each sits beside it, and
      the blind spot is written into `dependabot.yml` instead of left to be rediscovered.
      **Deliberately not done**: the `Dockerfile`'s `node:26-alpine` is still a moving tag. It was outside
      this item's scope and, unlike the scanners, it ships to production — pinning it trades "always patched"
      for "reproducible", which is a call to make on purpose rather than in passing (Dependabot's docker
      ecosystem would keep a `tag@digest` current if taken). The item's "consider" half — scanning the final
      image and publishing an SBOM — is also still undone.
- [x] **D4** · **CLOSED 2026-07-28** (`c1a9040`). `render.yaml` had stopped describing the deployed service:
      `LOKI_URL`, `LOKI_USERNAME`, `LOKI_PASSWORD` and `OTEL_METRICS_EXPORT_ENABLED` were set straight on the
      Render service when shipping was switched on and never added to the blueprint, so applying it to a
      fresh environment produced a service shipping **no logs and no metrics** — silently, because both
      pipelines are inert-when-unconfigured by design (D38). All four now declared, the three Loki values
      prompted (`sync: false`) like the OTLP endpoint already was. Checked the other direction as well: the
      remaining 24 keys the env schema accepts are absent on purpose, their defaults being what production
      wants. Note kept beside the variables: the Loki username is **not** the OTLP gateway's user id, and
      `LOKI_PASSWORD` is the same Grafana token as the one inside `OTEL_EXPORTER_OTLP_HEADERS`, so replacing
      it means editing both.

---

## E. Documentation drift

Verified stale on 2026-07-28. Several of these become correct only *after* the change is archived, so do this
pass last.

**`CLAUDE.md`**

- [x] **E1** Line ~57: "Green baseline as of 2026-07-27: **65 suites / 393 tests**, plus 8 e2e suites
      (34 tests)". Measured now: **70 suites / 461 unit tests**, **9 e2e suites / 44 tests**, 5 integration.
      *(Fixed 2026-07-28; unit counts re-verified by running the suite.)*
- [x] **E2** Line ~206: cache keys are described as `search:v1:<sha1>`. They are now
      `search:v1:<scope>:<sha1>`, and autocomplete is `ac:v1:<scope>:<sha1(prefix)>:<limit>` — the prefix is
      no longer embedded in clear. *(Fixed 2026-07-28, including what each scope digests and why they differ.)*
- [x] **E3** Lines ~16–21: "**four** archived changes", "**nine capabilities**", `D1–D34`. After archiving:
      five changes, ten capabilities (`telemetry-shipping` is new), and decisions run to **D38**.
      *(Done post-archive 2026-07-28, every figure re-measured: five archives listed, ten specs validated
      strict, D35–D38 grepped in the archive.)*
- [x] **E4** Lines ~345–346: "`openspec list` … reported no active changes". Untrue while this change is
      open, and it will need rewording once archived. *(Final wording landed post-archive 2026-07-28:
      no active changes as of the telemetry-shipping archive.)*
- [x] **E5** The **Health** bullet still says only that Elasticsearch is critical. It now also reports `down`
      when the configured index is missing — with the deploy consequence in E7. *(Fixed 2026-07-28, deploy
      consequence included; the README half of E7 remains open.)*
- [x] **E6** The authentication bullet should state the `API_KEYS` minimum length (16) now enforced at boot.
      *(Fixed 2026-07-28.)*

**`README.md`**

- [x] **E7** Document the readiness change and its operational consequence: a deployment against an unseeded
      Elasticsearch will never become healthy, so Render will fail the deploy rather than serve errors. That
      is the intended failure mode, but it means the seed must be guaranteed before boot and it is currently
      manual (`npm run seed:prod`). The env table and the `/health` example are already correct.
      *(Done 2026-07-28 in the `/health` section and deploy step 4.)*

**`docs/OBSERVABILITY-2026-07-27.md`**

- [x] **E8** Append the measured numbers from C/5.1. The document currently promises them.
      *(Done 2026-07-28 — plus the wire-format post-mortem and the keep-alive finding.)*

---

## F. How to verify each item

```bash
# Full local gate — what CI runs
npm run lint:ci && npm run test:cov && npm run build

# Against the real stack (must be up and seeded)
docker compose up -d elasticsearch redis
npx jest --config ./test/jest-integration.json
npx jest --config ./test/jest-e2e.json

# Deployed service
curl -s https://advanced-search-api-chet.onrender.com/health
curl -s -H "Authorization: Bearer $METRICS_TOKEN" \
  https://advanced-search-api-chet.onrender.com/metrics | grep -E 'http_requests_total|search_cache'
```

Grafana Cloud (stack `lankycliff422`), by datasource:

| Signal | Datasource | Query |
|---|---|---|
| Metrics | `grafanacloud-prom` | `{job="advanced-search-api"}` |
| Traces | `grafanacloud-traces` | `{resource.service.name="advanced-search-api"}` |
| Redis spans | `grafanacloud-traces` | `{resource.service.name="advanced-search-api" && span."db.system.name"="redis"}` |
| Logs (after B5) | `grafanacloud-logs` | `{service="advanced-search-api"} \| json \| correlationId="…"` |

---

## G. Traps found the hard way

Three measurement mistakes were made during this session. All three had the same shape — trusting a query
without checking that it measured what it appeared to — and they are recorded so the next session does not
repeat them.

1. **`span.db.system="redis"` can never match.** `@opentelemetry/instrumentation-ioredis` emits
   `db.system.name` (current semantic conventions) while `@elastic/transport` emits `db.system` (the older
   one). A query for the wrong attribute returns empty, which is indistinguishable from "no spans".
2. **TraceQL needs dotted attribute names quoted.** `span.db.system.name="redis"` returns nothing;
   `span."db.system.name"="redis"` works. The unquoted form parses as something else and fails silently.
3. **Probe traffic clusters around deploys.** Log lines and traces showing `/health` every 5 seconds were a
   deploy window, not steady state. Extrapolating that spacing across 24 hours produced a figure 170× too
   large and a finding that had to be withdrawn. Render does not poll `/health` continuously; the keep-alive
   cron does, six times an hour.

The general rule: an empty result is evidence about the query first and about the system second.

Two more from the 2026-07-28 activation session, same shape:

4. **Loki ingestion lags ~60–90 s behind the request.** The success lines "missing" from Loki minutes after
   the traffic were simply not ingested yet — the 4xx lines of the *same burst* had already appeared, which
   made the gap look selective. Re-querying two minutes later closed it. Re-measure before theorising about a
   partial drop.
5. **A silent transport must be proven, not configured.** Setting the Loki variables produced a healthy
   service whose stdout and Loki were both empty, with no error anywhere — `silenceErrors` doing its job on a
   batch that was never valid. The diagnosis that worked: reproduce with the real transport and real
   credentials, then bisect the config one knob at a time. The spec-level stubs could never have seen it.
6. **A verification window contaminated by your own traffic proves nothing.** The keep-alive fix looked
   verified 18 minutes after the deploy — until the instance napped at exactly 15 minutes past the last
   *manual* request, which is what had been holding it up. Any "it stayed alive" check must start *after*
   your own last request, not after the change. Same shape as the three above: the observation was real, the
   thing it measured was not the thing under test.

---

## H. New on 2026-07-28 — the keep-alive cron is throttled

- [x] **H1** · **CLOSED 2026-07-28** — GitHub runs the
      `*/10 * * * *` keep-alive roughly once an hour at best. The full run history sharpened the morning's
      estimate: gaps of 1–3.5 h routinely and up to **8h48**, with `createdAt == startedAt` and zero
      cancelled runs on every one — the ticks are never *created*, so no workflow setting can fix it, and it
      had been this way **since the cron shipped**. The "ticks land ~5 min late" belief came from watching
      two ticks on day one. That diagnosis is solid and is what the rewritten `keep-alive.yml` comments,
      CLAUDE.md and the README deploy notes now record.
      **Fix applied**: an external **UptimeRobot** monitor on `/health` every 5 minutes (3× inside Render's
      15-minute spin-down window), which also takes over the uptime-alarm role — the workflow's runs stayed
      green through every nap because its curl rides out the cold start it was meant to prevent.
      `keep-alive.yml` stays only as an hourly-ish backstop probe.
      **Verified 2026-07-28**, and the verification is worth reading because the first attempt at it was
      wrong. At **14:04:57 UTC the instance booted again** — new instance id, `/health` counter reset from
      251 to 14 — with no deploy and no cron run behind it: a spin-down, ~15 minutes after the last manual
      traffic at 13:49. Had the check been run a few minutes earlier, over a window that still contained
      that manual traffic, this would have been recorded as "fix verified". The nap was the monitor's own
      start-up gap: its **first check ever was at 14:04:22**, seconds before the instance went to sleep. From
      that point the boot lines stop: **18+ minutes of continuous uptime past the 14:19 spin-down point**,
      with the `/health` counter climbing monotonically — the monitor is holding the instance awake.
      **The trap, worth more than the fix**: UptimeRobot was first pointed at the service **root**, not
      `/health`. The root requires `X-API-Key` (the API has been private since 2026-07-27), so every check
      answered **401**, the monitor declared the service down and started emailing. Keeping the instance
      awake still worked — a 401 is inbound traffic and resets Render's idle timer just as a 200 does — so
      the *keep-alive* half was quietly fine while the *alarm* half was inverted: it now cries wolf forever,
      which is worse than the silent-green cron it replaced. It also spent rate-limit budget (`/` is not
      exempt; only `/health` is) and logged 401s under `route="/"`. Corrected to `/health` the same day.
      **When re-pointing this monitor, or adding another, the target must be `/health`** — the one endpoint
      the API-key guard and the rate limiter both skip, which is exactly why it exists.

- [x] **H2** · **Found by fixing H1.** **Measured fact**: while an instance is running, Render's own health
      check polls `/health` every **~4.3 s** — `increase(http_requests_total{route="/health"}[5m])` = 70.0 in
      steady state, well clear of any deploy window. **Projection, conditional on H1 actually landing**: an
      instance awake 24/7 takes that to ~20,160 requests/day, and every `/health` costs one Upstash `PING`,
      so it would spend **~605,000 Redis commands/month against the 500,000 free-tier allowance** — breach in
      ~25 days, before counting real cache traffic. While the naps continue the bill stays small, which is
      the only reason this is not already burning. **Fixing H1 is what makes H2 urgent**, so land them
      together rather than celebrating one and discovering the other.
      This *revises the G3 correction*: the withdrawn "~518k/month" estimate had the poll rate right and the
      duty cycle wrong — Render does poll continuously, but only while an instance is running, and the
      instance rarely ran. It also reopens **B4's** severity call, which silently assumed the naps.
      **Options when picked up** (do not improvise a fourth):
      **(a) Memoize the health report in-process.** The TTL sets the probe rate outright, so pick it with the
      arithmetic in hand — at a 4.3 s poll, probes/month ≈ `30 × 86400 / TTL`: a **5 s** TTL only skips every
      other poll (~302k/month — under the cap with no margin, so *not* enough on its own), **30 s** gives
      ~86k, **60 s** gives ~43k. The 30–60 s band is the useful one: 7–14× cheaper, and readiness stale by at
      most a minute against deploy decisions that take minutes anyway.
      **(b) Keep Redis out of the polled path.** Point Render's `healthCheckPath` at a variant that runs the
      ES/index probe only. Redis is non-critical by design — its `PING` never changes the status code Render
      acts on — so this takes the health-driven Redis cost to **zero** while preserving the index readiness
      that gates deploys (E7). Strictly the bigger lever; (a) still helps every other caller.
      **(c)** B4's full `/live` vs `/ready` split, the most invasive. (a) and (b) compose.
      **Also measure while there**: the same ~4.3 s cadence drives an `indices.exists` call against Elastic
      Cloud (~20k/day). Nobody has checked that against its quota — do not assume it is free, and do not
      assume it is a problem either.
      This is a behavior change against the synced `service-health` spec, so it gets a small OpenSpec change,
      not a quiet commit.

      **Shipped and verified in production 2026-07-28**: Render now polls `/health/ready` at one request
      every 4.2 s (`increase(...[5m])` = 71.25) while `route="/health"` reads **0**, so health-driven Redis
      traffic is down from ~605k commands/month to the monitor's ~8.6k. The breach is removed, not deferred.
      Only **6.5** remains before the change can be archived: what ~20k/day of `indices.exists` costs on the
      Elastic Cloud console, which needs a look at that console and is deliberately not assumed.

      **Taken 2026-07-28: option (c)**, as `split-health-liveness-and-readiness`. `/health/ready` runs the
      critical probes only and is what `render.yaml` polls, so health-driven Redis traffic goes to **zero**
      rather than being amortised; `/health` is untouched for humans and the monitor; `/health/live` calls
      nothing. Option (a) was rejected with a reason worth keeping: the 4.3 s `PING` is an accidental
      keepalive, so removing or thinning it lets the Upstash connection idle out and a status-based probe
      would flap through every reconnect. **Still open inside this**: the ES side is unchanged at ~20k
      `indices.exists`/day and remains unmeasured — task 6.5 of the change measures it before archiving.

- [ ] **H3** · Carried out of `split-health-liveness-and-readiness` when it was archived (its task 6.5).
      **Substance measured 2026-07-28; what remains is one glance at a console, not an investigation.**

      *The volume, re-verified the same day*: `increase(http_requests_total{route="/health/ready"}[5m])` =
      **70.0** — one poll every 4.29 s, **20,160/day, ~604,800/month** of `indices.exists` — while
      `route="/health"` reads **0**, so H2's split is still holding in production.

      *What it costs, established rather than assumed*: the target is an Elastic Cloud **Serverless**
      project, not a hosted deployment — the endpoint itself answers `build_flavor: serverless` (9.6.0).
      Elastic's published Serverless pricing bills VCU-hours (Ingest $0.14, Search $0.09, ML $0.07),
      storage $0.047/GB-month and egress $0.05/GB, and carries **no per-request, per-operation or per-call
      charge or quota whatsoever**. **The Upstash-shaped failure therefore cannot repeat here**: what bit
      this week was a per-command allowance that a constant poll exhausts silently, and that dimension does
      not exist on this side. Egress is bounded by arithmetic instead of by hope — `indices.exists` is a
      HEAD with no response body, so even at a generous 1 KB per reply the month is ~0.6 GB ≈ **$0.03**.

      *Storage, measured 2026-07-29 and now closed too*: `_cat/indices` still reports sizes on Serverless
      even though everything else does not. After the versioned migration the project holds two indices of
      24 documents — `products_v2` (`store` 97,600 B, `dataset` 1,362 B and climbing as it uploads) and the
      retained `products_v1` (`store` 0, `dataset` 99,368 B, already flushed to the object store). So the
      billed data is **~0.0002 GB ≈ $0.00001/month**, and D47's "double storage" trade-off is quantified
      rather than asserted: at this catalogue size retention costs nothing measurable.

      *Still unread, and the only thing left*: whether the constant poll holds **Search VCU** above the floor
      it would otherwise sit at. That is a consumption line rather than a quota, and it lives only in the
      Cloud console (Billing → Usage). It cannot be pulled from here, and that was verified rather than
      assumed: the project data key gets **401** from `api.elastic-cloud.com` (it is a data-plane key), and
      every usage API on the data plane — `_cluster/stats`, `_nodes/usage`, `_stats`, the metering
      endpoints — answers **410 Gone** on Serverless. Reading it needs the console or an *organization* API
      key.

      *Consequence for the fix this item pre-scoped*: memoizing the index check **loses its cost
      justification**. If it is ever done it will be for latency or robustness, not for quota. Do not
      re-litigate the endpoint split; it is settled and verified.

---

## Recommended order (updated at end of day)

1. **H3's last line** — one look at the Elastic Cloud console (Billing → Usage, the Search VCU trend).
   Confirmation, not investigation: the billing model is already established and rules out a quota breach.
   It is now the only item on this list that is still open.

Optional and not urgent — raised by D3, plus one carried out of B3:

- Pin the `Dockerfile`'s `node:26-alpine` by digest (a deliberate trade of "always patched" for
  "reproducible"), and add a final-image scan plus an SBOM.
- Automate the seed so a deploy cannot reach a cluster the migration has not run against. B3 shipped the
  migration itself but left the trigger manual on purpose; readiness already fails the deploy rather than
  serving errors, so this is a convenience, not a hole.

~~B5 + C~~, ~~E1–E8~~, ~~B1~~, ~~H1~~, ~~H2~~, ~~B4~~, ~~D3~~ — closed 2026-07-28. ~~B3~~ — closed
2026-07-29. See their sections.
