# Audit — 2026-07-23

An independent review of the deliverable: dependencies, tests, specs, documentation, container and HTTP
hardening, and the submission itself against the brief. **Every statement below was produced by running the
command named next to it**, not by reading the code and inferring. The companion load test is in
[`LOAD-TEST-2026-07-23.md`](LOAD-TEST-2026-07-23.md).

No application code was changed by this audit. The two defects it found in the documentation were fixed
separately in commit `2a5b7d5`.

## Verified green

| Area | Evidence | Result |
|---|---|---|
| Dependency vulnerabilities | `npm audit` | **0 vulnerabilities** |
| Unit tests | `npm test` | 35 suites, **106 tests**, all pass |
| End-to-end tests | `npm run test:e2e` | 6 suites, **19 tests**, all pass |
| Integration tests (real ES) | `npm run test:integration` | 1 suite, **5 tests**, all pass |
| Lint | `npm run lint` | clean, no warnings |
| Build | `npm run build` | clean (`strict` + `noUnused*` + `noImplicitReturns`) |
| Capability specs | `openspec validate --specs --strict` | **6/6 valid** — 22 requirements, 43 scenarios |
| Secret hygiene | `git check-ignore .env`, `git log --all -- .env` | ignored at `.gitignore:13`, **never committed** |
| Example env | inspection of `.env.example` | placeholders and `localhost` only, no real secrets |
| Container hardening | `Dockerfile` | non-root `USER node`, `tini` init, `--omit=dev` runtime, `HEALTHCHECK` |
| Postman collection | collection JSON | 14 requests, **14 with saved response examples** |
| File-size discipline | `wc -l` over `src/` | 87 production files, 2,469 lines, **largest 100** against a 250 cap |
| Runtime resilience | load battery | **0 failures in 366,306 requests** |

HTTP edge hardening was read and confirmed in `app.setup.ts` and `all-exceptions.filter.ts`: Helmet, env-aware
CORS, a global `ValidationPipe` with `whitelist` + `forbidNonWhitelisted` + `transform`, centralized error
mapping to typed bodies, and 5xx responses that log the stack server-side while returning a generic message.

## Findings

### F1 — `RENDER_API_KEY` lives in the application's `.env` · **High (operational)**

`.env` carries a Render platform API key that the application never reads: it appears nowhere in `src/`, and
`env.schema.ts` does not declare it, so Zod strips it. A deployment-platform credential with account-wide
reach is sitting in the file the API loads at boot, for no functional benefit.

It never reached the repository — `.env` is git-ignored and has never been committed — so the exposure is
local. It was nevertheless **printed in plaintext during this session**, so the key must be treated as
compromised.

**Do:** rotate the key now, and keep it in the MCP client configuration rather than the app's `.env`.

### F2 — No continuous integration · **Medium** · ✅ resolved

There was no `.github/workflows`: everything that guards quality — lint, three test suites, a type-checking
build — had to be remembered and run by hand.

**Resolved** in `.github/workflows/ci.yml`, on push and pull request to `main`. Two independent jobs, so a
lint failure cannot hide an e2e failure: `quality` runs lint, the unit suite and the build; `integration`
brings up Elasticsearch and Redis from the repo's own compose file (`--wait` for healthy), seeds the index and
runs the integration and e2e suites, dumping container logs if anything fails.

Fixing this exposed a second, quieter problem: `npm run lint` carries `--fix`, so in CI it would have repaired
an auto-fixable violation and reported success. Verified — a badly formatted file made `lint` exit **0** after
rewriting the file, while the new no-autofix `lint:ci` exited **1**. CI runs `lint:ci`.

### F3 — No rate limiting on a public read API · **Medium** · ✅ resolved

Every endpoint was a public unauthenticated `GET` fanning out to metered Elasticsearch and Redis, with the
existing guards limiting a request's *shape* (page size ⇒ 400, result window ⇒ 422) but not its *rate*. The
load test showed one laptop driving 1,593 req/s unopposed.

**Resolved** by the `add-request-rate-limiting` OpenSpec change: a global guard (`@nestjs/throttler`) with
per-endpoint budgets (60/min search and suggest, 300/min autocomplete, 120/min default), `/health` exempt.
The counter is in Redis for a shared limit and falls over to an in-process counter on a Redis outage — never
failing open, never failing closed, so Redis stays non-critical (D14). Over-budget returns the typed 429 with
`RateLimit-*` and `Retry-After` headers. Verified under load: with a 60-budget flood, exactly 60 served and
240 refused with a clean typed 429, rejection at 4.4 ms p95 — a flood cannot be turned into a slow-down. The
guard adds no measurable overhead when disabled (`search_cold` p95 30.4 ms vs the 29.7 ms baseline).

### F4 — The coverage number understates what is actually tested · **Low (reporting)**

`npm run test:cov` reports **64.76%** statements, and a reader would reasonably read that as a third of the
code being untested. The gap is an artifact of the setup: coverage runs only the unit suite, while the files
sitting at 0% are the composition roots — `main.ts`, `app.setup.ts`, `app.module.ts`, the five feature modules
and `config.module.ts` — which by construction can only be exercised by the e2e suite, and that suite is a
separate Jest project with no coverage collection.

The behaviour is covered; the metric just cannot see it.

**Do:** either exclude the wiring from `collectCoverageFrom` so the figure reflects testable code, or collect
coverage across the e2e project too. Until then, quote the number with its caveat.

### F5 — Direct dependencies sit one major behind · **Informational**

`npm outdated` lists ten majors available: `@elastic/elasticsearch` 8→9, `zod` 3→4, `jest` 29→30, `eslint`
9→10, `typescript` 5.9→7, and the type packages. None is a security issue (`npm audit` is clean), and the
Elasticsearch client **should** stay on 8.x to match the 8.17 server. This is recorded as a deliberate state,
not a defect.

## The deliverable against the brief

The lesson from the previous session — "implemented and tested" is not the same as "visible to a reviewer" —
was re-applied to every item the brief names:

| Brief asks for | State |
|---|---|
| Hexagonal architecture | Four layers, dependencies inward, ports as `Symbol` tokens, per-layer `README.md` |
| Elasticsearch relevance, filtering, faceting, suggestions | All four, in **one** round-trip per `/search` |
| Redis caching | Cache-aside, fail-open, verified against Redis counters in the load test |
| Docker + docker-compose | Multi-stage Dockerfile, compose with ES + Redis + API + one-shot seed profile |
| Seed script | Idempotent, 24/24 indexed, runs standalone from `dist/` in the container |
| README with install/run/test | Present, with an env table and a deploy walkthrough |
| Postman collection | 14 requests covering every search dimension, each with a saved response example |
| Online deployment | Live on Render, `/health` green, verified end to end in the smoke run |
| Error handling | Centralized filter, typed bodies, distinct 400/422/502/503 mapping |
| Env configuration | Zod-validated, fail-fast, no `localhost` assumption anywhere |
| Tests for core logic | 130 tests across unit, e2e and integration |

No gap of the kind found on 2026-07-22 was found this time.

## Recommendations, in order

1. ~~**Add a CI workflow**~~ — done, see F2.
2. ~~**Add rate limiting**~~ — done, see F3.
3. **Rotate the Render API key** and move it out of `.env` (F1). The owner has confirmed it is a test key and
   will rotate it at their discretion.
4. **Fix the coverage reporting** so the number means what a reader thinks it means (F4).
